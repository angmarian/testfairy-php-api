{
    "name": "TestFairy API (Build)",
    "description": "The TestFairy API (Build)",
    "operations": {
        "getBuilds": {
            "httpMethod": "GET",
            "parameters": {
                "project_id": {
                    "location": "uri",
                    "required": true,
                    "type": "string"
                }
            },
            "summary": "Gets a list of all builds",
            "uri": "projects/{project_id}/builds/"
        }
    }
}
